/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import NavApp from './src/App/NavApp';

AppRegistry.registerComponent(appName, () => NavApp);
